# Sahaiy Backend — Python Dependencies
# Install: pip install -r requirements.txt

# ── Web Framework ──────────────────────────────────────────────
fastapi==0.115.12
uvicorn[standard]==0.34.0
python-multipart==0.0.20      # For file uploads (STT endpoint)

# ── HTTP Client ────────────────────────────────────────────────
httpx==0.28.1                  # Async HTTP — STT, LLM, Sarvam TTS calls

# ── Supabase ───────────────────────────────────────────────────
supabase==2.15.2               # Server-side client (service role)

# ── Environment / Config ───────────────────────────────────────
python-dotenv==1.1.0

# ── Data Validation ────────────────────────────────────────────
pydantic==2.12.5

# ── RAG — Knowledge Base ───────────────────────────────────────
# Optional — only needed if you want RAG / knowledge base retrieval.
# Comment out if you don't need it.
faiss-cpu>=1.12.0
sentence-transformers==3.4.1
numpy==2.2.4

# ── LiveKit WebRTC (optional — Phase 10) ──────────────────────
# Server-side token route depends on livekit-api (imports as: from livekit import api)
livekit-api==0.8.2
# livekit-agents currently depends on watchfiles builds that fail on Python 3.14.
# Install automatically on supported versions only.
livekit-agents==0.8.11; python_version < "3.14"

# ── Utilities ──────────────────────────────────────────────────
anyio==4.9.0
starlette==0.46.1
