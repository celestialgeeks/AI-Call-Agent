# QA Evidence — Demo-Gate Suite (issue #9) — 2026-08-25

Owner: @qa-engineer · Branch: `test/issue-9-demo-gate-suite`

## 1. Python suites (contract + security + G4 WS) — against REAL in-process FastAPI app

Command:
```
./.venv/bin/python -m pytest qa/contract_tests qa/security_tests qa/ws_client --no-header
```

Actual output:
```
================== 24 passed, 6 xfailed, 9 warnings in 5.28s ===================
```

Breakdown:
- `qa/contract_tests`: **15 passed** — exact-shape conformance vs
  api-contracts-and-outreach-boundary-v1.md §1.1–§1.5 (+ outreach-boundary canary).
- `qa/security_tests`: **4 passed, 6 xfailed** — SEC-04/05/06 active;
  SEC-01/02/03 run as expected-fail canaries gated on `SAHAIY_JWT_ENFORCED=1`
  (they harden the moment @backend-eng's JWT dependency lands). The xfail for
  SEC-02 CONFIRMED the documented IDOR: call_end mutates another user's conversation.
- `qa/ws_client` (G4 primary path): **5 passed** — real WebSocket framing over a
  live uvicorn socket; transcript echo + LLM fragments + greeting-after-agent_meta
  all verified; turn completes well inside the 60s gate.

## 2. Playwright E2E vs LIVE prod (https://sahaiy.vercel.app)

```
cd qa/e2e && npx playwright test --reporter=list
```

Actual output (final run):
```
  5 failed
    G9 auth flows › OAuth google redirect lands in app   ← genuine bug
    G9 auth flows › OAuth github redirect lands in app   ← genuine bug
    G10: no hardcoded fake stats rendered                ← genuine bug
    G10: brand logos not presented as real customers     ← genuine bug
    G10: Terms/Privacy links resolve to real pages       ← genuine bug
  13 skipped        (env-gated: campaign UI / signed-in session / canaries)
  9 passed (33.5s)  (G1, G2, G6, G7 wired rows ×6)
```

Dead-row canaries (G7_CANARIES=1):
```
  8 passed, 6 failed — every failure = a dead control still visibly present,
  exactly as inventoried (START CALL orb, CONTACT SALES, footer BOOK A DEMO,
  DOCS/ENTERPRISE nav, Terms/Privacy). These flip to pass as fixes land.
```

## 3. Genuine bugs found by this suite (Section-1 format)

### BUG-1 [HONESTY] Fabricated stats still rendered on landing
Severity: High (release blocker class per plan: "fabricated status/data shown to users")
Environment: https://sahaiy.vercel.app @ main, Chromium headless 151
Suspected area: index.html hero/analytics sections

Repro: open landing page, read body text.
Expected: no fabricated metrics.
Actual: "98% CALL RESOLUTION RATE", "<200ms RESPONSE LATENCY", "2,847 CALLS TODAY",
"98.2% RESOLUTION RATE · Last 30 days · 124K calls", "50+ LANGUAGES" all hardcoded.

### BUG-2 [HONESTY] Real brand names rendered as customer logos
Severity: High — legal/reputation risk (inventory §honesty flags)
Repro: landing page, "TRUSTED BY TEAMS AT LEADING COMPANIES".
Actual: FLIPKART RAZORPAY ZEPTO PHONEPE MEESHO SWIGGY CRED GROWW rendered as text.
Expected: none until real customers exist.

### BUG-3 [LEGAL] Terms/Privacy links are dead "#"
Severity: High before public launch — DPDP compliance claim printed on same page.
Repro: landing footer + auth page footer links → href="#".

### BUG-4 [AUTH] Google/GitHub OAuth redirects never reach app.html
Severity: Medium-High (G9 gate fail)
Repro: auth.html → click Continue with Google/GitHub → wait 30s.
Actual: never navigates to app.html (Supabase redirect URL config, per inventory #7).

### BUG-5 [SECURITY-CONFIRMED] IDOR in call_end (from pytest SEC-02)
Severity: Critical pre-JWT-fix
Evidence: qa/security_tests xfail message — call_end with body user_id=A mutates
conversation owned by B (api-contracts "Auth model" warning reproduced).

## 4. Not yet runnable (blocked, by design)
- G3/G5 campaign UI specs — outbound UI not shipped (inventory #4).
- G4 supplementary mic run — needs real audio_meta path after deploy.
- G7 full coverage of dashboard rows — inventory v1 covers landing/auth only.
- TTS bars — ARM-wheel gate first (@devops-eng); fixtures already under qa/fixtures/.
